onst vW   = vH * (window.innerWidth / window.innerHeight);
        const ndcX = ((window.innerWidth  / 2 + gx) / window.innerWidth ) * 2 - 1;
        const ndcY = -((window.innerHeight / 2 + gy) / window.innerHeight) * 2 + 1;
        globalGlbCard.position.set(ndcX * vW / 2, ndcY * vH / 2, -DIST);
        
        if (globalGlbCard._baseScaleFact) {
          const _GS_SC  = 1.22;
          const _baseGSY = window.matchMedia('(max-width: 768px)').matches ? 0.90 : 1.0;
            const _t = Math.max(0, Math.min(1, (gSY - _baseGSY) / (_GS_SC - _baseGSY)));
          const _scSmall = globalGlbCard._baseScaleFact * (window.matchMedia('(max-width: 768px)').matches ? 0.5 : 0.65);
          const _scLarge = globalGlbCard._baseScaleFact * _GS_SC * (window.matchMedia('(max-width: 768px)').matches ? (1.0/_GS_SC) : 0.90);
          // Aplicamos el multiplicador inicial (1.5 -> 1.0)
          globalGlbCard.scale.setScalar((_scSmall + _t * (_scLarge - _scSmall)) * introState.scaleMult);
        }
