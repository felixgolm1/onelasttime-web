 (globalGlbCard && cardCanvasEl && document.getElementById('card-morph-group')) {
          const mgEl = document.getElementById('card-morph-group');

          mgEl.style.opacity = '0'; // CSS card siempre oculta — reemplazada por la 3D

          // ── CROSS