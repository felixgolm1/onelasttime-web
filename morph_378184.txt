e ese alto en pantalla.
    //    Usamos la altura real del card-morph-group en el momento de la carga.
    const mgEl0    = document.getElementById('card-morph-group');
    const mgRect0  = mgEl0 ? mgEl0.getBoundingClientRect() : null;
    const basePxH  = (m